# Write a program with a user defined
# function to count the number of times a
# character (passed as argument) occurs in
# the given string.
class First_String:
    # def count(name , character):
    #     c=0
    #     for i in name:
    #             if name == character :
    #                 c+=1
    #                 return "character of ",character,"is", c
    #
    # name= input("Enter the String:")
    #
    # character = input("Enter the character to be searched:")
    # char = count(name , character)
    # print(char)


    # Write  a program with a user defined function with string as a parameter which replaces all vowels in the string with '*'.
    # def second( name, special_character):
    #   #  vowels  = ["a", "e", "i", "o", "u"]
    #     for i in name:
    #         if i == "a" or i=="e" or i=="i" or i=="o" or i=="u":
    #             print("vowels:", i)
    #             name=name.replace(i, special_character)
    #
    #     return name
    #
    # name = input("Enter the string:")
    # special_character = input("Enter the * :")
    #
    # obj = second(name , special_character)
    # print("the string is replaced with * is :", obj)

# Write a program to input a string from
# the user and print it in the reverse order
# without creating a new string.
#         name = input("Enter the name:")
#         k=name[::-1]
#         print(k)
# Write a program which reverses a string
# passed as parameter and stores the
# reversed string in a new string. Use a user
# defined function for reversing the string.

    # Write  a  program using a user defined function   to check if a string is a  palindrome
    # or not.(A string is called palindrome if it reads same backwards as forward.For  example, Kanak is a palindrome.)

      #5)  # name = input("Enter the name:")
        # k=name[::-1]
        # if name == k:
        #     print("given string is polindrome")
        # else:
        #     print("not a polindrome")
     #6)
        # name ="welcome"
        # name=name.upper()
        # print("upper case of name is:", name)
        # name=name.lower()
        # print("lower case of name is :", name)
        # name=name.capitalize()
        # print("first letter uppercase is:", name)
        str= "abcdefghijklmnopqrstuvwxyz"
        print(str[1:-1])




